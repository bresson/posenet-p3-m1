import React, { useEffect, useState, useRef } from "react";
import * as tf from '@tensorflow/tfjs';


function buildModel(numOfFeatures) {
};

export function runTraining(convertedDatasetTraining, convertedDatasetValidation, numOfFeatures) {
  console.log('runTraining');
}